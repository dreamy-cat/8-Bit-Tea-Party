; Процедура расширенной печати в стандартный текстовый экран, параметры режима
;располагаются в отдельном файле или в системном заголовке. Формат строки 
;уже используем наш, а не стандартный. Выход за пределы буфера не допускается.
;Отсчет координат стандартный из верхнего левого угла (0, 0), смещение +00000h.

; Вход:
;AL	биты(флаги) управления:
;0	строка должна быть напечатана полностью или нет;
;1	используются координаты BL-BH или системного(своего) курсора;
;2	разрешен перенос символов на следующиую строку;
;3-7	пока что резерв;
;BL	координата по горизонтали в символах, без учета атрибутов;
;BH	координата по вертикали в символах;
;DS:DX	адрес структуры строки;
; Выход:
;AX	количество выведенных символов на экран(в следующию версию).

; В качестве демонстрации и отладки вывести строки со всеми предельными
;значениями по экрану и/или комбинации.

Print:		;Print[0,1,A...]
